{
  "name": "weda-rimc-landing",
  "version": "1.0.0",
  "private": true,
  "description": "WEDA Books RIMC landing page with Razorpay checkout (Vercel serverless).",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "engines": {
    "node": ">=18"
  }
}
